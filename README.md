# noise-and-filters

## Atividade 06

Façam um `fork` deste repositório, e implementem no arquivo `atividade06.py` as seguintes funções:

```python
def image_noise(noise, imagem):
    ''' noise é o tipo de ruído que será introduzido na imagem, podendo ser:
        - salt_and_pepper
        - white
        - gaussian
    '''
    return new image;


def image_filter(filter, image):
    ''' Os seguintes filtros deverão ser implementados:
        - mediana
        - gaussiano
        - mean3
        - mean5
    '''
    return new image;
```
